name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          path: main-repo
          
      - name: Checkout v1 branch
        uses: actions/checkout@v4
        with:
          ref: v1
          path: v1-repo

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - name: Run Bio-computational Tests
        run: |
          cd main-repo
          cargo test --workspace

      - name: Build main (Wasm and Assets to dist)
        run: |
          mkdir -p main-repo/dist
          cd main-repo/ui
          wasm-pack build --target web --out-dir ../dist/pkg
          cp -r assets ../dist/assets
          cp app.html ../dist/app.html
          cp simulador.html ../dist/simulador.html 2>/dev/null || true

      - name: Setup Bun (for v1)
        uses: oven-sh/setup-bun@v1

      - name: Build v1 (Vite)
        run: |
          cd v1-repo
          bun install
          bunx vite build --base=./
          
      - name: Arrange files
        run: |
          # Copy v1 dist into the main dist folder
          cp -r v1-repo/dist main-repo/dist/v1
          # Garantir que o GitHub Pages não processe com Jekyll
          touch main-repo/dist/.nojekyll

      - name: Build Documentation Pages (HTML to dist)
        run: |
          python3 -m pip install markdown
          python3 main-repo/scripts/build_docs.py "$GITHUB_WORKSPACE/main-repo/dist"

      - name: Deploy to KokoroSim.github.io
        env:
          DEPLOY_KEY: ${{ secrets.DEPLOY_KEY }}
        run: |
          mkdir -p ~/.ssh
          printf "%s\n" "$DEPLOY_KEY" > ~/.ssh/id_deploy
          chmod 600 ~/.ssh/id_deploy
          cd main-repo/dist
          echo "=== Arquivos na pasta dist a serem publicados ==="
          ls -la
          test -f index.html || { echo "❌ ERRO: index.html não foi encontrado na pasta dist!"; exit 1; }
          git init
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b main
          git add -A -f
          git commit -m "deploy: publicar KokoroSim no GitHub Pages [skip ci]"
          GIT_SSH_COMMAND="ssh -i ~/.ssh/id_deploy -o StrictHostKeyChecking=no" git push --force git@github.com:KokoroSim/KokoroSim.github.io.git main

