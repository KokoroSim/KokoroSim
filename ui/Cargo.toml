[package]
name = "ui"
version = "0.1.0"
edition = "2021"

[dependencies]
dioxus = { version = "0.5", features = ["web"] }
engine = { path = "../engine" }
gloo-timers = { version = "0.3", features = ["futures"] }
